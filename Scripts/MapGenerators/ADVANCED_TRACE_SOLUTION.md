# 🚀 高级追踪方案设计

## 📋 目标

让 `launch_generator.py` 能够精确追踪 `generate.py` 的执行情况：
- **追踪到模块级别**：知道正在执行哪个原子模块（.py文件）
- **追踪到代码行**：知道执行到模块的第几行
- **UE5.7.0 兼容**：在 UE5 Python 环境中正常工作

---

## 🎯 方案设计

### 方案架构图

```
┌─────────────────────────────────────────────────────────────┐
│  launch_generator.py (外部监控器)                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  1. 启动 UE5 进程                                      │  │
│  │  2. 实时读取输出流                                     │  │
│  │  3. 解析追踪标记                                       │  │
│  │  4. 显示执行状态                                       │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓ 读取输出
                    [TRACE:MODULE:xxx]
                    [TRACE:LINE:xxx]
                            ↑ 输出标记
┌─────────────────────────────────────────────────────────────┐
│  generate.py (内部被监控脚本)                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  generate/                                            │  │
│  │  ├── trace.py          ← 追踪工具模块                 │  │
│  │  ├── level_manager.py  ← 调用 trace.log_module()     │  │
│  │  ├── room_builder.py   ← 调用 trace.log_module()     │  │
│  │  ├── player_spawner.py ← 调用 trace.log_module()     │  │
│  │  └── ...               ← 所有模块都调用追踪           │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 实现方案

### 💡 问题1：打印 vs 传递信息

**当前方案：** 打印到 Log，`launch_generator.py` 解析输出流
**替代方案：** 直接传递信息（如 Socket、管道、共享内存）

**对比分析：**

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **打印到Log** | ✅ 简单<br>✅ UE5兼容<br>✅ 可调试<br>✅ 有历史记录 | ⚠️ 需要解析文本<br>⚠️ 有延迟 | ✅ **当前推荐** |
| **Socket通信** | ✅ 实时<br>✅ 结构化数据 | ❌ UE5 Python不支持socket<br>❌ 复杂 | ❌ 不可用 |
| **管道通信** | ✅ 快速 | ❌ UE5环境限制<br>❌ 跨进程复杂 | ❌ 不推荐 |
| **共享内存** | ✅ 最快 | ❌ UE5环境限制<br>❌ 同步复杂 | ❌ 不推荐 |

**结论：** 
- ✅ **继续使用打印到Log的方案**
- 原因：UE5 Python环境受限，Socket/管道/共享内存都不可用
- 优势：简单、可靠、有日志历史、易于调试

---

### 1️⃣ 增强 `generate/trace.py` 模块

**新增功能：模块级追踪**

```python
def log_module(module_name, line_num, context=""):
    """
    记录模块执行位置
    
    Args:
        module_name: 模块名称（如 "level_manager"）
        line_num: 模块内的行号
        context: 可选的上下文描述
    """
    marker = f"[TRACE:MODULE:{module_name}:LINE:{line_num}]"
    if context:
        marker += f" {context}"
    
    unreal.log(marker)
    print(marker, flush=True)
```

**使用示例（在各个模块中）：**

```python
# level_manager.py
from trace import log_module

def create_new_level(map_path):
    log_module("level_manager", 15, "开始创建Level")
    
    # ... 代码 ...
    log_module("level_manager", 25, "Level创建完成")
    
    return world
```

---

### 2️⃣ 增强 `launch_generator/trace_parser.py`

**新增：模块追踪信息存储**

```python
class TraceInfo:
    def __init__(self):
        # ... 现有字段 ...
        
        # 新增：模块追踪
        self.current_module = None      # 当前执行的模块名
        self.current_module_line = None # 当前模块的行号
        self.module_history = []        # 模块执行历史（完整记录，按时间顺序）
```

**💡 问题2：模块执行历史改为全部记录**

**改进：** 不限制只显示最近5个，而是记录**全部执行历史**，按时间顺序排序，允许重复调用。

```python
# 示例：完整的执行历史
trace_info.module_history = [
    {'module': 'level_manager', 'line': 15, 'timestamp': 1234567890.1},
    {'module': 'level_manager', 'line': 25, 'timestamp': 1234567890.2},
    {'module': 'room_builder', 'line': 10, 'timestamp': 1234567890.3},
    {'module': 'room_builder', 'line': 67, 'timestamp': 1234567890.4},
    {'module': 'level_manager', 'line': 30, 'timestamp': 1234567890.5},  # 重复调用
    {'module': 'player_spawner', 'line': 34, 'timestamp': 1234567890.6},
    # ... 全部记录，不限制数量
]
```

**新增：模块标记解析**

```python
def _parse_trace_marker(line, trace_info):
    """解析追踪标记"""
    try:
        # 新增：解析模块标记
        if '[TRACE:MODULE:' in line:
            # 格式: [TRACE:MODULE:level_manager:LINE:25] 创建Level
            parts = line.split('[TRACE:MODULE:')[1].split(']')[0]
            module_parts = parts.split(':LINE:')
            
            if len(module_parts) == 2:
                module_name = module_parts[0]
                line_num = int(module_parts[1])
                
                trace_info.current_module = module_name
                trace_info.current_module_line = line_num
                
                # 记录历史
                trace_info.module_history.append({
                    'module': module_name,
                    'line': line_num,
                    'timestamp': time.time()
                })
        
        # ... 现有的其他标记解析 ...
        
    except:
        pass
```

---

### 3️⃣ 增强输出显示

**实时显示模块执行状态：**

```python
# 在 process_runner.py 中
def print_trace_info(trace_info):
    """打印追踪信息"""
    print(f"\n📍 执行追踪:")
    
    # 显示当前模块
    if trace_info.current_module:
        print(f"  当前模块: {trace_info.current_module}.py")
        print(f"  模块行号: {trace_info.current_module_line}")
    
    # 显示最后函数
    if trace_info.last_function:
        print(f"  最后函数: {trace_info.last_function}")
    
    # 显示最后检查点
    if trace_info.last_checkpoint:
        print(f"  最后检查点: {trace_info.last_checkpoint}")
    
    # 显示完整模块执行历史（全部，按时间顺序）
    if trace_info.module_history:
        print(f"\n  📜 模块执行历史（共 {len(trace_info.module_history)} 条，按执行顺序）:")
        for i, entry in enumerate(trace_info.module_history, 1):
            # 显示序号、模块名、行号
            print(f"    {i:3d}. {entry['module']}.py:L{entry['line']}")
```

---

## 📊 输出效果示例

### 场景1：首次生成地图

```
[15:30:45] 启动UE5...
(静默运行中，请等待...)

============================================================
  执行摘要 (压缩)
============================================================
[1/6] 创建Level
[2/6] 构建训练室
[3/6] 放置PlayerStart
[4/6] 设置照明
[5/6] 配置GameMode
[6/6] 保存地图

构建进度:
  步骤完成: 6/6 (100%)
    ✓ [1/6]
    ✓ [2/6]
    ✓ [3/6]
    ✓ [4/6]
    ✓ [5/6]
    ✓ [6/6]
  Actors创建: 15/15 (100%)

📍 执行追踪:
  当前模块: map_saver.py
  模块行号: 42
  最后函数: save_map() - 地图保存成功
  最后检查点: MAP_SAVED

  📜 模块执行历史（共 23 条，按执行顺序）:
      1. level_manager.py:L15
      2. level_manager.py:L25
      3. level_manager.py:L30
      4. room_builder.py:L10
      5. room_builder.py:L25
      6. room_builder.py:L45
      7. room_builder.py:L67
      8. room_builder.py:L80
      9. player_spawner.py:L12
     10. player_spawner.py:L34
     11. player_spawner.py:L45
     12. lighting_system.py:L15
     13. lighting_system.py:L30
     14. lighting_system.py:L51
     15. game_mode_config.py:L10
     16. game_mode_config.py:L25
     17. game_mode_config.py:L40
     18. map_saver.py:L15
     19. map_saver.py:L28
     20. map_saver.py:L42
     21. generator.py:L50
     22. generator.py:L65
     23. main.py:L30

✓ 成功: 地图文件已生成
  路径: D:\001xm\shijiewuxian\Content\Maps\Cosmos_002_Training_World.umap
  文件大小: 1,234,567 bytes (1205.63 KB)
  修改时间: 2025-12-18 15:32:10
============================================================
```

### 场景2：重新生成地图（有文件大小对比）

```
[16:45:20] 启动UE5...
(静默运行中，请等待...)

============================================================
  执行摘要 (压缩)
============================================================
[1/6] 创建Level
[2/6] 构建训练室
[3/6] 放置PlayerStart
[4/6] 设置照明
[5/6] 配置GameMode
[6/6] 保存地图

构建进度:
  步骤完成: 6/6 (100%)
    ✓ [1/6]
    ✓ [2/6]
    ✓ [3/6]
    ✓ [4/6]
    ✓ [5/6]
    ✓ [6/6]
  Actors创建: 18/18 (100%)

📍 执行追踪:
  当前模块: map_saver.py
  模块行号: 42
  最后函数: save_map() - 地图保存成功
  最后检查点: MAP_SAVED

  📜 模块执行历史（共 25 条，按执行顺序）:
      1. level_manager.py:L15
      2. level_manager.py:L25
      3. level_manager.py:L30
      4. room_builder.py:L10
      5. room_builder.py:L25
      6. room_builder.py:L45
      7. room_builder.py:L67
      8. room_builder.py:L80
      9. room_builder.py:L95
     10. player_spawner.py:L12
     11. player_spawner.py:L34
     12. player_spawner.py:L45
     13. lighting_system.py:L15
     14. lighting_system.py:L30
     15. lighting_system.py:L51
     16. lighting_system.py:L65
     17. game_mode_config.py:L10
     18. game_mode_config.py:L25
     19. game_mode_config.py:L40
     20. map_saver.py:L15
     21. map_saver.py:L28
     22. map_saver.py:L42
     23. generator.py:L50
     24. generator.py:L65
     25. main.py:L30

✓ 成功: 地图文件已生成
  路径: D:\001xm\shijiewuxian\Content\Maps\Cosmos_002_Training_World.umap
  旧文件大小: 1,234,567 bytes (1205.63 KB)
  新文件大小: 1,456,789 bytes (1422.64 KB)
  大小变化: +222,222 bytes (+216.99 KB, 18.0% 增大)
  旧修改时间: 2025-12-18 15:32:10
  新修改时间: 2025-12-18 16:47:35
============================================================
```

**💡 问题3：文件大小对比说明**

**当前实现：**
- ✅ **已经有文件大小对比功能**（在 `result_analyzer.py` 中）
- 首次生成：只显示文件大小和修改时间
- 重新生成：显示旧/新文件大小对比、大小变化、修改时间对比

**示例文档中的错误：**
- ❌ 示例只显示了首次生成的情况
- ✅ 现在已更新为两个场景：首次生成 + 重新生成

---

## 🎯 优势

### ✅ 精确定位
- 知道执行到哪个模块
- 知道模块的哪一行
- 完整的执行历史

### ✅ UE5 兼容
- 使用 `unreal.log()` 输出
- 不依赖 `sys.settrace()`
- 不依赖 `threading`

### ✅ 低侵入性
- 只需在关键位置调用 `log_module()`
- 不影响原有代码逻辑
- 可以随时开关

### ✅ 易于调试
- 出错时立即知道位置
- 可以回溯执行路径
- 方便定位问题

---

## 📝 实施步骤

### 步骤1：增强 trace.py
- 添加 `log_module()` 函数
- 保持向后兼容

### 步骤2：在各模块中添加追踪
- `level_manager.py`：在关键位置调用
- `room_builder.py`：在关键位置调用
- `player_spawner.py`：在关键位置调用
- 其他模块同理

### 步骤3：增强 trace_parser.py
- 添加模块追踪字段
- 添加模块标记解析
- 记录执行历史

### 步骤4：增强输出显示
- 显示当前模块
- 显示模块行号
- 显示执行历史

### 步骤5：测试验证
- 运行地图生成
- 验证追踪输出
- 确认信息准确

---

## 🔍 追踪标记格式规范

```
[TRACE:MODULE:模块名:LINE:行号] 可选的上下文描述

示例：
[TRACE:MODULE:level_manager:LINE:25] 创建Level完成
[TRACE:MODULE:room_builder:LINE:67] 加载Cube网格
[TRACE:MODULE:player_spawner:LINE:34] 放置PlayerStart
```

---

## 💡 扩展可能

### 未来可以添加：
- **性能追踪**：记录每个模块的执行时间
- **内存追踪**：记录内存使用情况
- **API调用追踪**：记录UE5 API调用次数
- **错误追踪**：记录错误发生的模块和行号
- **可视化**：生成执行流程图

---

## 📌 注意事项

1. **追踪点不要太密集**：避免影响性能
2. **关键位置必须追踪**：函数入口、API调用、检查点
3. **保持格式统一**：便于解析
4. **添加有意义的上下文**：便于理解

